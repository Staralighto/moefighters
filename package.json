{
  "name": "moefighters",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "node --experimental-strip-types scripts/selfcheck.ts",
    "sheets": "node --experimental-strip-types scripts/write-sheets.ts"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
